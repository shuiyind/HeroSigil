# Task 2.1.5: en_us.json 本地化补充 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补充 en_us.json 本地化条目，添加缺失的 tooltip 和成就翻译（英文版本）

**Architecture:** 在 en_us.json 中添加新的本地化键值对，更新现有不匹配的条目

**Tech Stack:** JSON, Minecraft Localization

---

## 任务文件分析

**源文件:** `C:\Users\shuiy\Desktop\CodeSYD\HeroSigil\src\main\resources\assets\herosigil\lang\en_us.json`

**当前状态:** 已有部分本地化条目，但缺少任务要求的新条目

**任务文件:** `C:\Users\shuiy\Desktop\CodeSYD\HeroSigil\docs\tasks\task-2.1-gui-visual.md`

---

## 任务 2.1.5: en_us.json 本地化补充

**Files:**
- Modify: `C:\Users\shuiy\Desktop\CodeSYD\HeroSigil\src\main\resources\assets\herosigil\lang\en_us.json`

### 步骤 1: 分析现有 en_us.json 文件

**Files:**
- Read: `C:\Users\shuiy\Desktop\CodeSYD\HeroSigil\src\main\resources\assets\herosigil\lang\en_us.json`

**目标:** 了解现有本地化条目，确定需要新增和修改的条目

**现有条目需要修改:**
1. `tooltip.herosigil.requirement` - 需添加占位符 `%s`
2. `tooltip.herosigil.active_status` - 需修改格式
3. `achievement.herosigil.explore_biomes` - 需添加 `explore_all_biomes` 键

**需要新增的条目:**
1. `tooltip.herosigil.click_to_deactivate`
2. `tooltip.herosigil.duration_info`
3. `tooltip.herosigil.amplifier`
4. `tooltip.herosigil.progress`

### 步骤 2: 分析现有 en_us.json 文件内容

**Files:**
- Read: `C:\Users\shuiy\Desktop\CodeSYD\HeroSigil\src\main\resources\assets\herosigil\lang\en_us.json`

**目标:** 确认现有条目，与 zh_cn.json 保持同步

### 步骤 3: 新增缺失的本地化条目

**Files:**
- Modify: `C:\Users\shuiy\Desktop\CodeSYD\HeroSigil\src\main\resources\assets\herosigil\lang\en_us.json`

**新增条目:**
```json
{
    // ... 现有条目 ...
    
    "tooltip.herosigil.click_to_deactivate": "Click to deactivate this buff",
    "tooltip.herosigil.duration_info": "Duration: %d seconds",
    "tooltip.herosigil.amplifier": "Amplifier Level: %d",
    "tooltip.herosigil.progress": "%d/%d"
}
```

### 步骤 4: 修改现有不匹配的条目

**Files:**
- Modify: `C:\Users\shuiy\Desktop\CodeSYD\HeroSigil\src\main\resources\assets\herosigil\lang\en_us.json`

**修改 1: requirement**
```json
// 修改前
"tooltip.herosigil.requirement": "Requires achievement completion"

// 修改后
"tooltip.herosigil.requirement": "Requires achievement: %s"
```

**修改 2: active_status**
```json
// 修改前
"tooltip.herosigil.active_status": "%s Active"

// 修改后
"tooltip.herosigil.active_status": "Active status: %s"
```

**修改 3: 成就键名**
```json
// 修改前
"achievement.herosigil.explore_biomes": "Explore the World"

// 修改后 - 添加新键并保留旧键作为兼容
"achievement.herosigil.explore_all_biomes": "Explore All Biomes"
```

### 步骤 5: 验证 JSON 格式

**Files:**
- Check: `C:\Users\shuiy\Desktop\CodeSYD\HeroSigil\src\main\resources\assets\herosigil\lang\en_us.json`

**执行:**
```powershell
cd C:\Users\shuiy\Desktop\CodeSYD\HeroSigil
Get-Content src/main/resources/assets/herosigil/lang/en_us.json | ConvertFrom-Json
```

**预期结果:** JSON 格式正确，无解析错误

### 步骤 6: 提交代码

**Files:**
- Git: `C:\Users\shuiy\Desktop\CodeSYD\HeroSigil`

**执行:**
```powershell
cd C:\Users\shuiy\Desktop\CodeSYD\HeroSigil
git add src/main/resources/assets/herosigil/lang/en_us.json
git commit -m "$(cat <<'EOF'
feat(localization): 补充 en_us.json 本地化条目

- 新增 click_to_deactivate (停用提示)
- 新增 duration_info (持续时间信息)
- 新增 amplifier (放大器等级)
- 新增 progress (进度显示)
- 修改 requirement 添加占位符
- 修改 active_status 格式
- 新增 explore_all_biomes 成就键名

EOF
)"
```

---

## 自检查清单

- [x] 新增 `click_to_deactivate` 条目
- [x] 新增 `duration_info` 条目
- [x] 新增 `amplifier` 条目
- [x] 新增 `progress` 条目
- [x] 修改 `requirement` 添加占位符
- [x] 修改 `active_status` 格式
- [x] 新增 `explore_all_biomes` 成就键
- [x] JSON 格式正确
- [x] 所有条目使用英文
- [x] 与 zh_cn.json 保持同步

## 文件路径确认

- 修改文件: `C:\Users\shuiy\Desktop\CodeSYD\HeroSigil\src\main\resources\assets\herosigil\lang\en_us.json`

---

**计划完成。** 执行时请按步骤顺序执行，每步完成后验证结果。
